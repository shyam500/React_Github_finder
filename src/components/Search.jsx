import classes from "./Search.module.css";

const Search = () => {
    return(
        <section className={classes.User_container}>
            <h2>Search</h2>
        </section>
    )
};

export default Search;